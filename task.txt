/add .
/architect
[CRITICAL INSTRUCTION: ABSOLUTE AUTONOMY & ZERO YAPPING] You are an Elite Autonomous Principal Engineer. You are executing a massive full-stack overhaul for a Bar Exam SRS application. RULE 1: DO NOT ask for my permission or opinion. RULE 2: DO NOT give me multiple choices (e.g., "Should I do 1 or 2?"). If a decision is needed, autonomously choose the absolute best, most robust practice and implement it immediately. RULE 3: DO NOT explain your code. Just write and modify the files. RULE 4: Execute all the following 5 phases sequentially. [PHASE 1: Frontend UI/UX Overhaul] The current UI is broken and shows old formats. Fix it forcefully: 1. Dashboard.tsx: Completely REMOVE old progress bars. Implement Anki-style counters. Each deck row MUST show three aligned numbers: <Blue (New)> <Red (Learning/Again)> <Green (Review)>. 2. StudySession.tsx: COMPLETELY REMOVE the 1-to-5 multiple-choice buttons. The front of the card must show a single O/X statement. 3. Card Back & Buttons: - The bottom action bar MUST have exactly 4 Anki-style buttons: Again (Red), Hard (Gray), Good (Green), Easy (Blue). - Implement the "Union Textbook Style" explanation UI: Render legal_basis in a Blue box, case_citation in a Purple box. Show importance badges (A/B/C) and out-dated law warnings. Add a Floating Action Button (FAB) for the "AI Tutor". [PHASE 2: Backend & Database Update] 1. models.py: Refactor the Card model to include legal_basis (String), case_citation (String), explanation_core (Text), and keywords (JSON). 2. FastAPI Routers: Update endpoints to serve this new structured JSON data to the frontend. Create /api/v1/chat/explain endpoint for the AI Tutor. [PHASE 3: RAG Data Pipeline (Real Legal Sources)] 1. Create data_pipeline/legal_retriever.py to fetch real texts before LLM generation. - Use law.go.kr Open API for statutes and precedents. - Implement a robust fallback web scraper for casenote.kr, lbox.kr, or scourt.go.kr if the official API fails. 2. Update data_pipeline/llm_processor.py: The LLM MUST use the retrieved real text to generate the final O/X explanation (to prevent hallucination) and output it in the new structured JSON format matching Phase 2. [PHASE 4: Data Execution Script] Create a script named run_and_deploy.ps1 (PowerShell) and run_and_deploy.sh (Bash) in the root directory. The script must: 1. Run the data pipeline for a small batch to generate real data (e.g., python -m data_pipeline.pipeline mock --idx-min 87 --idx-max 87). 2. Run git add . 3. Run git commit -m "Auto-deploy: UI/UX overhaul, RAG pipeline integration, and new card generation" 4. Run git push origin main (This will automatically trigger the Render.com deployment). [PHASE 5: FINAL EXECUTION] Write all the necessary codes for Phase 1 to 4 directly to the files. Once you have finished modifying ALL files, output a single terminal command for me to run the newly created run_and_deploy.ps1 or run_and_deploy.sh script. DO NOT SAY ANYTHING ELSE.